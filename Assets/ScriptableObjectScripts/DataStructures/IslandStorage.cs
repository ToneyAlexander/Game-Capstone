using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName = "DataStructures/IslandStorage")]

public class IslandStorage : ScriptableObject
{
    public string name = "Scary Monsters and Super Creeps";
    public int theme = 0;
    public int size = 20;
    public int height = 5;
    public int level = 1;
    public int boss = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
